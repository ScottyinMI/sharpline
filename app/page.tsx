// app/page.tsx — Sharpline Daily Game View (M6 Tasks 1, 2, 3)
// Server component shell — DashboardPage is the client boundary.

import DashboardPage from './dashboard/DashboardPage';

export default function Home() {
  return <DashboardPage />;
}
